app = "milesminder"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  DB_PATH = "/data/milesminder.db"

[[mounts]]
  source = "milesminder_data"
  destination = "/data"

[processes]
  app = "python -m milesminder.bot"
